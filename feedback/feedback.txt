відсутня умова завдання в гіті

struct User { - ми ж обговорили чому тут має бути клас - оновити
class ViewController: UIViewController { - codeStyle - default names не годиться

знову accessModifiers

    @IBOutlet weak var googleButton: UIButton!
    @IBOutlet weak var hello: UILabel!
    @IBOutlet weak var logoImageView: UIImageView!


навіщо 2-чі             DispatchQueue.main.async { ??


            DispatchQueue.main.async {
                if let fullname = userObj?.fullname,
                    let email = userObj?.email {
                    self?.hello.text = "Привіт, \(fullname), \(email)"
                }
                DispatchQueue.main.async {


Є доречі AlamofireImage - можна його використати було б


в h файлі не потрібно робити видимими всі методи класу - треба інкапсулювати логіку

пусті методи - видалити


codeStyle:

if ([self.operation isEqualToString:@"+"]) {
        result = self.firstNumber + self.secondNumber;
    }
    else if

    ==>


if ([self.operation isEqualToString:@"+"]) {
    result = self.firstNumber + self.secondNumber;
} else if


у обєктів є tag властивість - туди можна було б записати номер кнопки і зробити перерахування де по індексу кнопки була б задана функціональність - краща альтернативе порівнянню title на кнопці - адже шо станеться якшо зміниться локалізація або просто треба буде замінити напис на кнопці - вся логіка поламається

також можна було б використати IBOutletCollection(UIButton), взагалі логіка базується на штерфейсній частині - тут велика звязка

спробуй замінити написи на кнопках так шоб скрізь всі символи були б в лапках - все додаток не працює

ну і +1 за максимально простий варіант, проте не певний шо основні нюанси роботи з памяттю були зрозумілі - завдання виконано по найпростішому шляху )